# aeroroute-mlx-training

Offline, reproducible dataset generation, QLoRA experiments, evaluation, model
cards, and adapter publication. There is no production HTTP service here.
Base weights, generated datasets, and unreviewed artifacts are not committed.
